<!DOCTYPE html>
<html lang="en">
<%- include('./includes/head') %>
<body>
  <%- include('./includes/nav') %>
  <h1>List of Items: <%= listAnimals.length %></h1>
  <% if(listAnimals.length != 0) { %>
    <ul class="no-style row wrap list-shop">
      <% for(let animal of listAnimals){ %>      
        <li>
          <%- include('./includes/item') %>
        </li>
      <% } %>
    </ul>
  <% } else { %>
    <p>List of animals empty</p>  
  <% } %>
</body>
</html>